import { BodyColumnDirective } from './body-column.directive';

describe('BodyColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyColumnDirective();
    expect(directive).toBeTruthy();
  });
});
