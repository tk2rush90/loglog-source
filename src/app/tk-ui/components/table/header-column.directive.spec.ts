import { HeaderColumnDirective } from './header-column.directive';

describe('HeaderColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderColumnDirective();
    expect(directive).toBeTruthy();
  });
});
